server:
  port: 8090

spring:
  application:
    name: hmdp-ai-service
  # 目前主模型使用 DashScope；OpenAI 相关 starter 已移除

  # 阿里云 通义千问 Flash
  ai:
    dashscope:
      api-key: sk-e3179f706a1646ee82eb4920d1bf0a1e
      chat:
        options:
          model: qwen-turbo-flash
          temperature: 0.2

logging:
  level:
    com.hmdp.ai: info