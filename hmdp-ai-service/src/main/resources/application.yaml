server:
  port: 8090

spring:
  application:
    name: hmdp-ai-service
  ai:
    openai:
      api-key: ${OPENAI_API_KEY:}
      chat:
        options:
          model: ${OPENAI_MODEL:gpt-4o-mini}
          temperature: 0.2

logging:
  level:
    com.hmdp.ai: info

